name: A11Y

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  call-core:
    uses: ./.github/workflows/core-tests.yml
    permissions:
      contents: write
    with:
      suite: a11y
      projects: "A11Y"
