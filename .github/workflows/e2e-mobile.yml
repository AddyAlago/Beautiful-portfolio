name: E2E Mobile

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  call-core:
    uses: ./.github/workflows/core-tests.yml
    permissions:
      contents: write
    with:
      suite: mobile
      projects: "Mobile E2E"
